import { Readable } from 'stream';
export declare class FileOutput extends Readable {
    private url_;
    private blob_;
    constructor(url: string);
    url(): string;
    blob(): Promise<Blob>;
    _read(size: number): void;
}
